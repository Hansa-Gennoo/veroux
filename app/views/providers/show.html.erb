<h1><%= @provider.display_name %></h1>
<p><%= @provider.bio %></p>

<h2>Services</h2>

<% if @services.any? %>
  <ul>
    <% @services.each do |service| %>
      <li><%= service.title %></li>
    <% end %>
  </ul>
<% else %>
  <p>No published services yet.</p>
<% end %>
