<header style="margin-bottom: 18px;">
  <h1 style="font-size: 22px; margin: 0;">Bookings</h1>
  <p style="margin: 6px 0 0; color: rgba(0,0,0,.65);">
    A clear view of what’s scheduled — and what needs your attention.
  </p>
</header>

<% if @bookings.any? %>
  <section style="display: grid; gap: 12px;">
    <% @bookings.each do |booking| %>
      <%= render "booking", booking: booking %>
    <% end %>
  </section>
<% else %>
  <section style="padding: 18px; border: 1px dashed rgba(0,0,0,.18); border-radius: 16px; background: rgba(0,0,0,.02);">
    <h2 style="font-size: 16px; margin: 0 0 6px;">No bookings yet</h2>
    <p style="margin: 0 0 14px; color: rgba(0,0,0,.65);">
      Once clients book a service, you’ll see them here.
    </p>
    <%= link_to "Create a service", new_dashboard_service_path,
      style: "text-decoration:none; display:inline-block; font-size: 13px; padding: 10px 14px; border-radius: 14px; border: 1px solid rgba(0,0,0,.14); background: #fff;" %>
  </section>
<% end %>
